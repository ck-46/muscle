<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MUSCLE STORE｜商品ページ</title>
    <style>
    ul li {
        list-style: none;
    }
    </style>
</head>
<body>
    <header>
        {% include 'header.html.twig' %}
    </header>
    <main>
        <div>
            {% for value in dataArr %}
            <div>
                <ul>
                    <li>
                        <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}detail.php?item_id={{value.item_id}}">{{value.item_name}}</a>
                    </li>
                    <li>
                        <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}detail.php?item_id={{value.item_id}}">
                            <img src="{{constant('main\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}">
                        </a>
                    </li>
                    <li>
                        &yen;{{value.price|number_format(0, '.',',')}}
                    </li>
                    <li>
                        <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}detail.php?item_id={{value.item_id}}">詳細</a>
                    </li>
                </ul>
            </div>
            {% endfor %}
        </div>
    </main>
    <footer>
        {% include 'footer.html.twig' %}
    </footer>
</body>
</html>