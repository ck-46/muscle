<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{constant('main\\Bootstrap::ENTRY_URL')}}stylesheets/index.css" rel="stylesheet">
    <title>MUSCLE STORE｜アカウント情報編集ページ</title>
</head>
<body>
<header>
    {% include 'header.html.twig' %}
</header>
<main class="container">
    <form method="post" action="update_account_confirm.php">
        <h2 class="h2 mt-4 mb-4">アカウント情報編集</h2>
        <p>変更したい情報を入力してください</p>
        {% if errArr.all | length > 0 %}<div class="text-danger">{{errArr.all}}</div>{% endif %}
        <div class="form-group">
            <label for="family_name">氏：{{dataArr.family_name}}</label>
            <input type="text" id="family_name" class="form-control" name="family_name" value="{{updateDataArr.family_name}}" size="40">
        </div>
        <div class="form-group">
            <label for="first_name">名：{{dataArr.first_name}}</label>
            <input type="text" id="first_name" class="form-control" name="first_name" value="{{updateDataArr.first_name}}" size="40">
        </div>
        <div class="form-group">
            <label for="email">メールアドレス：{{dataArr.email}}</label>
            {% if errArr.email | length > 0 %}<div class="text-danger">{{errArr.email}}</div>{% endif %}
            <input type="text" id="email" class="form-control" name="email" value="{{updateDataArr.email}}" size="40">
        </div>
        <div class="form-group">
            <label for="password">パスワード：******</label>
            <div>※パスワードは6〜20文字の半角数字で入力してください</div>
            {% if errArr.password | length > 0 %}<div class="text-danger">{{errArr.password}}</div>{% endif %}
            <input type="password" id="password" class="form-control" name="password" value="" size="40">
        </div>
        <input type="submit" class="btn btn-primary" name="confirm" value="変更を確認">
        </form>
</main>
<footer>
    {% include 'footer.html.twig' %}
</footer>
</body>
</html>