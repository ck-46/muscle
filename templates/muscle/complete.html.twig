
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MUSCLE STORE｜完了ページ</title>
</head>
<body>
    <header>
        {% include 'header.html.twig' %}
    </header>
    <main>
        <div>
            <h2>{{ content.type }}完了しました</h2>
            {% if  content.page | length > 0 %}
                {# ログインページURL #}
                <div><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}login.php">{{ content.page }}</a></div>
            {% endif %}
            {% if content.name | length > 0 %}
                <div>こんにちは、{{ content.name }}さん</div>
            {% endif %}
            <div><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}index.php">トップページへ</a></div>
        </div>
    </main>
    <footer>
        {% include 'footer.html.twig' %}
    </footer>
</body>
</html>