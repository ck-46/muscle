<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{constant('main\\Bootstrap::ENTRY_URL')}}stylesheets/index.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    <title>MUSCLE STORE｜レビュー履歴ページ</title>
</head>
<body>
<header>
    {% include 'header.html.twig' %}
</header>
<main class="container">
    <section id="review_list" class="pt-4">
        <h2 class="font-weight-bold mt-4 mb-4">レビュー履歴</h2>
        {% if reviewList | length == 0 %}
            <p>レビュー履歴はありません</p>
        {% else %}
            {% for key, value in reviewList %}
                <div class="card mb-3">
                    <div class="card-body">
                        <h5 class="card-title font-weight-bold">{{value.item_name}}</h5>
                        <p class="card-text text-right"><small class="text-muted">{{value.review_date | date("Y年m月d日H時i分")}}</small></p>
                        <p class="card-text">内　　容：{{value.content}}</p>
                        <p class="card-text">いいね数：{{value.good}}</p>
                        <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}review_list_update.php?review_id={{value.review_id}}&mode=update" class="card-link">編集する</a>
                        <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}review_list_update.php?review_id={{value.review_id}}&mode=delete" class="card-link">削除する</a>
                    </div>
                </div>
            {% endfor %}
        {% endif %}
        <div class="mt-3">
            <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}mypage.php">マイページへ戻る</a></p>
        </div>
    </section>
</main>
<footer>
    {% include 'footer.html.twig' %}
</footer>
</body>
</html>