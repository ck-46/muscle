<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{constant('main\\Bootstrap::ENTRY_URL')}}stylesheets/index.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    <title>MUSCLE STORE｜カート</title>
</head>
<body>
<header>
    {% include 'header.html.twig' %}
</header>
<main class="container">
    <section id="cart" class="pt-4">
        <h2 class="font-weight-bold mt-4 mb-4">{{ user_name }}さんのカート</h2>
        {% if dataArr | length == 0 %}
            <p>カートに商品は入っていません</p>
        {% else %}
            <div class="card mb-4">
                <div class="card-body">
                    <p class="card-text">カート内商品数：{{sumNum}}個</p>
                    <p class="card-text">合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
                    <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}buy.php" class="btn btn-primary">購入する</a>
                </div>
            </div>
            <div class="row justify-content-start mb-4">
                {% for value in dataArr %}
                    <div class="card col-11 col-md-5 col-lg-3 mx-3 mb-2 p-0" style="min-width: 18rem;">
                        <img src="{{constant('main\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" class="card-img-top" alt="{{value.item_name}}">
                        <div class="card-body">
                            <h5 class="card-title font-weight-bold">{{value.item_name}}</h5>
                            <p class="card-text">価格：&yen;{{value.price|number_format(0, '.', ',')}}</p>
                            <p class="card-text">数量：{{value.num}}</p>
                            <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}cart.php?del_item_id={{value.item_id}}" class="btn btn-secondary">削除</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </section>
    <div class="mt-3">
        <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
    </div>
    <div class="mt-3">
        <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}mypage.php">マイページへ戻る</a></p>
    </div>
</main>
<footer>
    {% include 'footer.html.twig' %}
</footer>
</body>
</html>