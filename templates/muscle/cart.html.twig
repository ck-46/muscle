<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MUSCLE STORE｜カート</title>
    <style>
    ul li {
        list-style: none;
    }
    </style>
</head>
<body>
    <header>
        {% include 'header.html.twig' %}
    </header>
    <main>
        <div id="cart_list">
            <p>{{ user_name }}さんのカート</p>
            <p>カート内商品数：{{sumNum}}個　合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
            {% if dataArr | length == 0 %}
                <p>カートに商品は入っていません</p>
            {% else %}
                {% for value in dataArr %}
                    <div>
                        <ul>
                            <li>
                                <img src="{{constant('main\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}">
                            </li>
                            <li>
                                {{value.item_name}}
                            </li>
                            <li>
                                &yen;{{value.price|number_format(0, '.', ',')}}
                            </li>
                            <li>
                                <a href="{{constant('main\\Bootstrap::ENTRY_URL')}}cart.php?crt_id={{value.crt_id}}">削除</a>
                            </li>
                        </ul>
                    </div>
                {% endfor %}
            {% endif %}
            <p><a href="">購入する</a></p>
            <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
        </div>
    </main>
    <footer>
        {% include 'footer.html.twig' %}
    </footer>
</body>
</html>