<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{constant('main\\Bootstrap::ENTRY_URL')}}stylesheets/index.css" rel="stylesheet">
    <title>MUSCLE STORE｜カート</title>
    <style>
    ul li {
        list-style: none;
    }
    </style>
</head>
<body>
<header>
    {% include 'header.html.twig' %}
</header>
<main class="container">
    <h2 class="h2 mt-4 mb-4">{{ user_name }}さんのカート</h2>
    <p>カート内商品数：{{sumNum}}個　合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
    {% if dataArr | length == 0 %}
        <p>カートに商品は入っていません</p>
    {% else %}
        {% for value in dataArr %}
            <div>
                <ul>
                    <li>
                        <img src="{{constant('main\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}">
                    </li>
                    <li>
                        {{value.item_name}}
                    </li>
                    <li>
                        &yen;{{value.price|number_format(0, '.', ',')}}
                    </li>
                    <li>
                        <a class="btn btn-secondary" href="{{constant('main\\Bootstrap::ENTRY_URL')}}cart.php?crt_id={{value.crt_id}}">削除</a>
                    </li>
                    <li>
                        <a class="btn btn-primary" href="{{constant('main\\Bootstrap::ENTRY_URL')}}}">購入</a>
                    </li>
                </ul>
            </div>
        {% endfor %}
    {% endif %}
    <p><a href="">購入する</a></p>
    <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
</main>
<footer>
    {% include 'footer.html.twig' %}
</footer>
</body>
</html>