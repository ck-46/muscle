<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{constant('main\\Bootstrap::ENTRY_URL')}}stylesheets/index.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    <title>MUSCLE STORE｜カート</title>
    <style>
    ul li {
        list-style: none;
    }
    </style>
</head>
<body>
<header>
    {% include 'header.html.twig' %}
</header>
<main class="container">
    <h2 class="h2 mt-4 mb-4">{{ user_name }}さんのカート</h2>
    {% if dataArr | length == 0 %}
        <p>カートに商品は入っていません</p>
    {% else %}
        <p>カート内商品数：{{sumNum}}個　合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
            <p><a class="btn btn-primary" href="{{constant('main\\Bootstrap::ENTRY_URL')}}buy.php">購入する</a></p>
            {% for value in dataArr %}
                <div>
                    <ul>
                        <li>
                            <img src="{{constant('main\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}">
                        </li>
                        <li>
                            {{value.item_name}}
                        </li>
                        <li>
                            &yen;{{value.price|number_format(0, '.', ',')}}
                        </li>
                        <li>
                            個数：{{value.num}}
                        </li>
                        <li>
                            <a class="btn btn-secondary" href="{{constant('main\\Bootstrap::ENTRY_URL')}}cart.php?del_item_id={{value.item_id}}">削除</a>
                        </li>
                    </ul>
                </div>
            {% endfor %}
    {% endif %}
    <p><a href="{{constant('main\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
</main>
<footer>
    {% include 'footer.html.twig' %}
</footer>
</body>
</html>